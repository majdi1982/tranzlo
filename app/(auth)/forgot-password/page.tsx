import { SectionPage } from "@/components/marketing/section-page";

export default function ForgotPasswordPage() {
  return (
    <SectionPage title="Forgot password" description="Recover access to your Tranzlo account.">
      <p>Reset-link flow will be wired to Appwrite authentication when credentials are available.</p>
    </SectionPage>
  );
}

